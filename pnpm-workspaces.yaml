packages:
  # all packages in direct subdirs of packages/
  - "packages/*"
  # all config packages, such as 'tsconfig', 'prettier', etc
  - "packages/config/*"
  # all packages in subdirs of apps/
  - "apps/*"
  # exclude packages that are inside test directories
  - "!**/test/**"
